source("renv/activate.R")

options(repos = c(CRAN = "https://cran.rstudio.com/"))

if (interactive()) {
  suppressMessages(require(devtools))
  devtools::dev_sitrep()
}

if (interactive()) {
  source("~/.Rprofile")
}
